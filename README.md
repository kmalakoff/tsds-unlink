# tsds-unlink

Internal `ts-dev-stack` command adapter for removing a linked package from `node_modules`.

Install and run the parent CLI instead:

```bash
npm install --save-dev ts-dev-stack
tsds unlink
```

Use `tsds link` to create the corresponding link. This package is not a standalone end-user command.
